/**
 * Default Messages API model when `ANTHROPIC_MODEL` is unset.
 * Override with env if your org pins a different snapshot (see Anthropic model IDs).
 */
export const DEFAULT_ANTHROPIC_MODEL = "claude-sonnet-4-6" as const;
